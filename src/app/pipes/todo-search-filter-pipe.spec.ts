import { TodoSearchFilterPipe } from './todo-search-filter-pipe';

describe('TodoSearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TodoSearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
